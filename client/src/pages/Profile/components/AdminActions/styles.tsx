import styled from "styled-components";

export const AdminSBContainer = styled.div`
    padding: 16px;
    border-radius: 6px;
    background: ${props => props.theme.background};
    box-shadow: 0 0 10px ${props => props.theme.shadow};
`